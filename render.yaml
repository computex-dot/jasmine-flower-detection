services:
  - type: web
    name: jasmine-flower-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && pip install -r yolov7/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free